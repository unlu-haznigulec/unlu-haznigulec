import 'package:flutter/material.dart';

class PListTile extends StatelessWidget {
  const PListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
